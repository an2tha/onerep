<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
		Written out rather than generated. GENERATE_INFOPLIST_FILE is off for
		this target because the keys below (WKApplication, the workout
		background mode) have no build-setting equivalent, which means the
		standard bundle keys Xcode would otherwise inject have to be spelled
		out here too — omit CFBundleIdentifier and the app builds fine and then
		refuses to install.
	-->
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>OneRep</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>WKApplication</key>
	<true/>
	<key>WKCompanionAppBundleIdentifier</key>
	<string>com.ananthh.onerep</string>
	<key>WKRunsIndependentlyOfCompanionApp</key>
	<false/>
	<!--
		Keeps the app alive while a workout is in progress and the wrist is
		down. Without it the session is suspended mid-set and the heart-rate
		samples stop, which is the whole reason to run on a watch at all.
	-->
	<key>WKBackgroundModes</key>
	<array>
		<string>workout-processing</string>
	</array>
	<key>NSHealthShareUsageDescription</key>
	<string>OneRep reads your heart rate and active energy during a workout so it can show live stats and save an accurate session.</string>
	<key>NSHealthUpdateUsageDescription</key>
	<string>OneRep saves workouts you record on your watch to Health.</string>
</dict>
</plist>
