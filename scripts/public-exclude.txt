# Paths stripped from the public GitHub mirror by scripts/publish-github.sh.
# One repo-relative path per line; blank lines and #-comments are skipped.
# These exist in the internal repo's history — which is exactly why the mirror
# gets a generated history instead of this one.
convex/billing/_private
apps/mobile/src/components/billing/_private
# The marketing site stays internal; the public repo is the app, not the pitch.
apps/web
# The CI config names deployments, buckets, and signing references. Harmless on
# its own, a map of the estate to anyone who collects such things.
codemagic.yaml
