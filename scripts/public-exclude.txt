# Paths stripped from the public GitHub mirror by scripts/publish-github.sh.
# One repo-relative path per line; blank lines and #-comments are skipped.
# These exist in the internal repo's history — which is exactly why the mirror
# gets a generated history instead of this one.
convex/billing/_private
apps/mobile/src/components/billing/_private
# The marketing site stays internal; the public repo is the app, not the pitch.
apps/web
# CI config. Deleted from the tree, but it lived here long enough to be in the
# history, and the history is what gets published. It named deployments and
# signing references — harmless on its own, a map of the estate to anyone who
# collects such things.
codemagic.yaml
# The Actions workflows run in the private repo. The mirror has no use for the
# dispatch buttons, and less for the list of secret names.
.github/workflows
